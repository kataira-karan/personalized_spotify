import React from "react";

const Home = () => {
  const redirectToSpotify = () => {};

  return (
    <div className="home-container">
      <button onClick={() => redirectToSpotify}>Login</button>
    </div>
  );
};

export default Home;
